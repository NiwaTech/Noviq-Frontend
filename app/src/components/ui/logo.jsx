export default function Logo( {className=''} ) {
    return (
        <div className={`flex size-7 items-center justify-center rounded-full ${className} bg-[#3d0fa8] text-[13px] font-semibold text-white`}>
            N
        </div>
    )
}